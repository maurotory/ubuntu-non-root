# ubuntu-non-root

Run in docker
`docker run -it mauritori/busybox:latest bash`

Or in kubernetes:
`kubectl apply -f https://raw.githubusercontent.com/maurotory/ubuntu-non-root/refs/heads/master/busybox-nonroot.yaml`

